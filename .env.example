# =============================================================================
# Steampunk Strategy — The Bridge
# Environment Variables Template
# =============================================================================

# Database (Neon PostgreSQL — dedicated instance, separate from Studiolo/Postmaster)
DATABASE_URL="postgresql://user:password@ep-xyz.us-west-2.aws.neon.tech/strategy?sslmode=require"

# NextAuth
NEXTAUTH_URL="http://localhost:3000"
NEXTAUTH_SECRET="generate-with-openssl-rand-base64-32"

# Azure AD (shared app registration with Studiolo/Postmaster)
AZURE_AD_CLIENT_ID=""
AZURE_AD_CLIENT_SECRET=""
AZURE_AD_TENANT_ID=""

# Anthropic (Claude AI — document parsing, expense categorization, compliance suggestions)
ANTHROPIC_API_KEY=""

# Gmail API (receipt scanning — Amazon, Chewy, etc.)
GOOGLE_CLIENT_ID=""
GOOGLE_CLIENT_SECRET=""
GOOGLE_REFRESH_TOKEN=""

# Vercel Blob (scanned document storage)
BLOB_READ_WRITE_TOKEN=""

# Vercel API (site monitoring dashboard — read-only personal access token)
VERCEL_API_TOKEN=""

# Stripe (donation tracking sync from Studiolo)
STRIPE_SECRET_KEY=""

# Studiolo API (cross-site data sync — donor-paid vendor bills, gift tracking)
STUDIOLO_API_URL="https://steampunkstudiolo.org"
STUDIOLO_SYNC_SECRET=""

# Postmaster API (animal data for transparency reporting)
POSTMASTER_API_URL="https://postmaster.steampunkstudiolo.org"

# Cleanpunk Shop / Medusa (COGS tracking, inventory costs)
MEDUSA_BACKEND_URL="https://cleanpunk-shop.medusajs.app"
MEDUSA_PUBLISHABLE_KEY=""
