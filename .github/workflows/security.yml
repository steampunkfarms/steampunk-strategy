name: Security

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  gitleaks:
    name: Secret Scan
    uses: steampunkfarms/shared-workflows/.github/workflows/gitleaks.yml@main
    secrets: inherit

  security:
    name: NPM Audit
    uses: steampunkfarms/shared-workflows/.github/workflows/security-scan.yml@main
    secrets: inherit
